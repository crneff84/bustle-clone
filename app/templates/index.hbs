<div class="spacer">
  {{#link-to 'admin'}}Admin{{/link-to}}
</div>
<div class="row specialArticles">
  {{#each specialarticles as |special|}}
    <div class="col-sm-4">
      {{thumbnail-display specialArticle=special}}
    </div>
  {{/each}}
</div>
<div class="row articles">
  <div class="col-sm-12">
    {{#each articles as |article|}}
      {{banner-display article=article}}
    {{/each}}
  </div>
</div>
<div class="spacer">
</div>
<div class="row">
  {{#each articles as |article|}}
    {{article-list article=article}}
  {{/each}}
</div>
